VAULT_ADDR=http://192.168.1.200:8200
VAULTWARDEN_PORT=8080

# These are rendered by Vault Agent into /run/secrets/app.env at runtime
# ADMIN_TOKEN=        ← from Vault: secret/vaultwarden/config
# DATABASE_URL=       ← from Vault: secret/vaultwarden/config

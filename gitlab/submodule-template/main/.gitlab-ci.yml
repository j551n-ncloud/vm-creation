---
# GITOPS_REPO_PATH must be set as a GitLab CI variable for this project,
# e.g. mygroup/proxmox-gitlab-stack
variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - validate

include:
  - project: "${GITOPS_REPO_PATH}"
    ref: main
    file: "gitlab/templates/validation.yml"
